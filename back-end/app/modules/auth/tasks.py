# TODO: add Celery tasks for auth domain (e.g., send welcome email, password-reset email).
# When adding, declare time_limit and soft_time_limit, keep tasks idempotent, and use
# Redis locks for any shared-resource mutations (see CLAUDE.md security rules).
