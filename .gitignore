# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml
*.ipr
*.iws

# Environment
.env
.env.*
!.env.example

# Local-only working notes / handovers (não versionar)
no-git/

# Firebase service account (Admin SDK) — segredo, nunca versionar
**/firebase-service-account*.json
secrets/

# Firebase saiu do app na spec A (ver docs/front-end/firebase_setup.md). As
# entradas ficam porque um checkout antigo ainda pode ter esses arquivos no
# disco, e nenhum deles deve ser versionado se a spec C reintroduzir push.
front-end-flutter/lib/firebase_options.dart
front-end-flutter/firebase.json
front-end-flutter/android/app/google-services.json
**/GoogleService-Info.plist

# web-admin (Angular)
web-admin/node_modules/
web-admin/dist/
web-admin/.angular/
